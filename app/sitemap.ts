import type {MetadataRoute} from 'next';import {advisors,guides} from '@/lib/data';export default function sitemap():MetadataRoute.Sitemap{const base='https://advisor-registry.example';return ['','/advisors','/match','/guides','/reviews','/for-advisors',...advisors.map(a=>`/advisors/${a.slug}`),...guides.map(g=>`/guides/${g.slug}`)].map(url=>({url:base+url,lastModified:new Date()}))}
