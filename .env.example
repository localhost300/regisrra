# Set this to the final production origin before deployment.
NEXT_PUBLIC_SITE_URL=https://www.advisorregistry.wiki

# Server-only email settings for the contact form.
SMTP_HOST=smtp.us.appsuite.cloud
SMTP_PORT=465
SMTP_USER=Info@advisorregistry.wiki
SMTP_PASSWORD=replace-with-your-mailbox-password
CONTACT_EMAIL=Info@advisorregistry.wiki
