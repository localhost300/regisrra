# Advisor Registry

Premium financial advisor marketplace built with Next.js, TypeScript, and Tailwind CSS.

## Run locally

1. Run `npm install`.
2. Run `npm run dev`.

The interface uses fictional demonstration advisors and runs without authentication or external services.
