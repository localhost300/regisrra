import Link from 'next/link'; import {Star,ShieldCheck,MapPin,ArrowRight} from 'lucide-react'; import type {Advisor} from '@/lib/data';
export function Stars({rating,size='sm'}:{rating:number;size?:'sm'|'lg'}){return <div className="rating"><Star size={size==='lg'?18:14} fill="currentColor"/><b>{rating.toFixed(1)}</b></div>}
export function AdvisorCard({a}:{a:Advisor}){return <article className="advisor-card"><div className="advisor-top"><div className="avatar">{a.initials}</div><div><div className="eyebrow">{a.verified&&<><ShieldCheck size={14}/> Public record located</>}</div><h3>{a.name}</h3><p>{a.title}</p><p className="firm">{a.firm}</p></div><button className="save" aria-label="Save advisor">♡</button></div><div className="card-meta">{a.reviews>0?<><Stars rating={a.rating}/><span>{a.reviews} reviews</span></>:<span>No platform reviews</span>}<span><MapPin size={14}/>{a.location}</span></div><div className="tags">{a.specialties.map(x=><span key={x}>{x}</span>)}</div><p className="bio">{a.bio}</p><div className="details"><span>{a.years}+ years experience</span><span>{a.fee}</span>{a.fiduciary&&<span>Fiduciary</span>}</div><div className="card-actions"><Link className="btn secondary" href={`/advisors/${a.slug}`}>View profile</Link><Link className="btn" href={`/advisors/${a.slug}#contact`}>Connect</Link></div></article>}
export function SectionHead({kicker,title,copy,link}:{kicker?:string;title:string;copy?:string;link?:string}){return <div className="section-head"><div>{kicker&&<p className="kicker">{kicker}</p>}<h2>{title}</h2>{copy&&<p>{copy}</p>}</div>{link&&<Link href={link}>View all <ArrowRight size={16}/></Link>}</div>}
export function Logo(){return <Link className="logo" href="/" aria-label="Advisor Registry home"><span className="mark" aria-hidden="true"><svg viewBox="0 0 40 40" fill="none"><circle cx="20" cy="20" r="17.5" stroke="currentColor"/><path d="M20 8.5 24.4 20 20 31.5 15.6 20 20 8.5Z" fill="currentColor"/><path d="M8.5 20h7.1m8.8 0h7.1" stroke="currentColor" strokeLinecap="round"/><circle cx="20" cy="20" r="2.3" fill="var(--mark-core)"/></svg></span><span className="wordmark"><b>Advisor</b><i>Registry</i></span></Link>}
