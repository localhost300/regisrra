'use client';
import {useEffect,useRef,useState} from 'react';import {MessageSquare,X} from 'lucide-react';import ContactForm from './ContactForm';
export default function ContactModal({advisorName}:{advisorName:string}){const [open,setOpen]=useState(false);const closeRef=useRef<HTMLButtonElement>(null);useEffect(()=>{if(!open)return;closeRef.current?.focus();const onKey=(e:KeyboardEvent)=>{if(e.key==='Escape')setOpen(false)};document.addEventListener('keydown',onKey);document.body.style.overflow='hidden';return()=>{document.removeEventListener('keydown',onKey);document.body.style.overflow=''}},[open]);return <><button className="btn profile-connect" onClick={()=>setOpen(true)}><MessageSquare size={17}/> Connect with {advisorName.split(' ')[0]}</button>{open&&<div className="modal-backdrop" role="presentation" onMouseDown={e=>{if(e.target===e.currentTarget)setOpen(false)}}><section className="contact-modal" role="dialog" aria-modal="true" aria-labelledby="contact-modal-title"><div className="modal-head"><div><p className="kicker">Private inquiry</p><h2 id="contact-modal-title">Connect with {advisorName}</h2></div><button ref={closeRef} className="modal-close" onClick={()=>setOpen(false)} aria-label="Close contact form"><X/></button></div><ContactForm advisorName={advisorName}/></section></div>}</>}
