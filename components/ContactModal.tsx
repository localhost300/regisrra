'use client';
import {useEffect,useRef,useState} from 'react';import {MessageSquare,X} from 'lucide-react';import ContactForm from './ContactForm';
export default function ContactModal({advisorName,advisorSlug,contactEnabled}:{advisorName:string;advisorSlug:string;contactEnabled:boolean}){const [open,setOpen]=useState(false);const closeRef=useRef<HTMLButtonElement>(null);useEffect(()=>{if(!open)return;closeRef.current?.focus();const onKey=(e:KeyboardEvent)=>{if(e.key==='Escape')setOpen(false)};document.addEventListener('keydown',onKey);document.body.style.overflow='hidden';return()=>{document.removeEventListener('keydown',onKey);document.body.style.overflow=''}},[open]);return <><div className="profile-connect-wrap"><button className="btn profile-connect" onClick={()=>setOpen(true)}><MessageSquare size={17}/> Schedule a call with {advisorName.toUpperCase()}</button>{contactEnabled&&<p>{advisorName} will respond to you within 24–48 business hours.</p>}</div>{open&&<div className="modal-backdrop" role="presentation" onMouseDown={e=>{if(e.target===e.currentTarget)setOpen(false)}}><section className="contact-modal" role="dialog" aria-modal="true" aria-labelledby="contact-modal-title"><div className="modal-head"><div><p className="kicker">Private inquiry</p><h2 id="contact-modal-title">{contactEnabled?`Schedule a call with ${advisorName}`:'Contact unavailable'}</h2></div><button ref={closeRef} className="modal-close" onClick={()=>setOpen(false)} aria-label="Close contact form"><X/></button></div>{contactEnabled?<ContactForm advisorName={advisorName} advisorSlug={advisorSlug}/>:<div className="contact-card"><p>Direct inquiries are not currently available for this profile.</p></div>}</section></div>}</>}
