'use client'; import {useState} from 'react'; import Link from 'next/link'; import {Menu,X} from 'lucide-react'; import {Logo} from './ui';
const links=[['Find an Advisor','/match'],['Explore Advisors','/advisors'],['Financial Guides','/guides'],['Reviews','/reviews']];
export default function Nav(){const [open,setOpen]=useState(false);return <header className="nav"><div className="nav-inner"><Logo/><nav>{links.map(([n,h])=><Link key={h} href={h}>{n}</Link>)}</nav><div className="nav-actions"><Link href="/for-advisors">For Advisors</Link><Link className="btn small" href="/match">Find your advisor</Link></div><button className="menu" onClick={()=>setOpen(!open)} aria-label="Toggle menu">{open?<X/>:<Menu/>}</button></div>{open&&<div className="mobile-nav">{[...links,['For Advisors','/for-advisors']].map(([n,h])=><Link onClick={()=>setOpen(false)} key={h} href={h}>{n}</Link>)}<Link className="btn" href="/match">Find your advisor</Link></div>}</header>}
